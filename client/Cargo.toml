[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", features = ["wayland"] }
rand = "0.10.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
